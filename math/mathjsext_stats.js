'use strict';

(function () {

    function center(X) {
        var mean = math.mean(X);
        var centeredX = math.subtract();
    }

    function covariance(X, Y) {
        var mx = math.mean(X);
        var my = math.mean(Y);
    }

})();